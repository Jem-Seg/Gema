# ==========================================
# GESTOCK - Variables d'environnement
# ==========================================
# Copier ce fichier en .env et remplir les valeurs

# ==========================================
# DATABASE
# ==========================================
# SQLite (développement)
DATABASE_URL="file:./prisma/dev.db"

# PostgreSQL (production - recommandé)
# DATABASE_URL="postgresql://user:password@localhost:5432/gestock?schema=public"

# ==========================================
# NEXTAUTH CONFIGURATION
# ==========================================
# URL de l'application (IMPORTANT : changer en production)
# Développement local :
NEXTAUTH_URL=http://localhost:3000

# Production (exemples) :
# NEXTAUTH_URL=https://gestock.votredomaine.com
# NEXTAUTH_URL=http://192.168.1.100:3000

# Secret pour signer les tokens JWT (GÉNÉRER UN NOUVEAU EN PRODUCTION !)
# Générer avec : openssl rand -base64 32
NEXTAUTH_SECRET=CHANGER_CETTE_VALEUR_EN_PRODUCTION

# ==========================================
# ADMIN SECURITY
# ==========================================
# Clé secrète pour créer le premier administrateur
# Générer avec : openssl rand -base64 32
ADMIN_SECRET_KEY=CHANGER_CETTE_VALEUR_EN_PRODUCTION

# ==========================================
# EMAIL CONFIGURATION (Réinitialisation mot de passe)
# ==========================================
# Configuration SMTP pour l'envoi d'emails
# Pour Gmail: https://support.google.com/accounts/answer/185833 (Créer un mot de passe d'application)
# Pour autres: Utilisez les paramètres SMTP de votre fournisseur

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com

# Nom de l'application affiché dans les emails
NEXT_PUBLIC_APP_NAME=GeStock

# ==========================================
# OPTIONAL - STORAGE (si cloud storage)
# ==========================================
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# AWS_BUCKET_NAME=

# ==========================================
# NODE_ENV
# ==========================================
# development | production | test
NODE_ENV=development

# ==========================================
# LOGGING CONFIGURATION
# ==========================================
# Niveau de log minimum à enregistrer
# DEBUG (développement) | INFO (production) | WARN | ERROR | FATAL
LOG_LEVEL=INFO

# Répertoire des fichiers de log
LOG_DIR=./logs

# Taille maximale d'un fichier de log en MB (rotation automatique)
LOG_MAX_FILE_SIZE=10

# Nombre maximum de fichiers de log à conserver (suppression automatique)
LOG_MAX_FILES=30

# Activer la sortie console des logs
LOG_ENABLE_CONSOLE=true

# Activer l'enregistrement dans des fichiers
LOG_ENABLE_FILE=true

# ==========================================
# UPLOADS CONFIGURATION
# ==========================================
# Utiliser stockage local (public/uploads) - uniquement en développement
# En production, laisser vide pour utiliser dossier externe
USE_LOCAL_UPLOADS=true

# Chemin personnalisé pour uploads (optionnel)
# Si non défini, utilise :
#   - Windows : C:\gestock\uploads
#   - macOS/Linux : ~/gestock/uploads
# UPLOADS_DIR=
